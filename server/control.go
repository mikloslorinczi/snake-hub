package server

import (
	"github.com/gorilla/websocket"
)

func newUser(conn *websocket.Conn) error {
	// msg := modell.ServerMsg{
	// 	Msg : ""
	// }
	// cmd := modell.CommandObj{}
	// if err := conn.ReadJSON(cmd); err != nil {
	// 	return errors.Wrap(err, "Cannot sync with client")
	// }
	// if cmd.Command == "Join" {

	// }
	return nil
}
